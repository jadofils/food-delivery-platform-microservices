<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>food-delivery.Platform</groupId>
		<artifactId>fdp</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<artifactId>notification-service</artifactId>
	<name>notification-service</name>
	<description>FDP notification/audit log, consumes domain events. See docs/services/notification-service.md.</description>

	<dependencies>
		<dependency>
			<groupId>food-delivery.Platform</groupId>
			<artifactId>common</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- MongoDB, not JPA/Postgres: notification_db is domain data (RULES.md §5) but this is
			 the one FDP service whose datastore is MongoDB, not Postgres, per RULES.md §2. No
			 Flyway either, that rule is Postgres-specific (RULES.md §5), Mongo collections are
			 created implicitly on first write. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-amqp</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<!-- Distributed tracing (RULES.md §13): Boot 4.1 split tracing autoconfiguration into its
			 own starter, declaring micrometer-tracing-bridge-brave/zipkin-reporter-brave directly
			 pulls in the bridge library but not the autoconfiguration that actually wires the Tracer/
			 Zipkin-reporting beans (confirmed empirically: no span ever reached Zipkin with just
			 those two). This starter is what spring-boot-dependencies' own BOM defines for this.
			 Version managed by Boot's own parent BOM (RULES.md §4). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-zipkin</artifactId>
		</dependency>
		<!-- Version managed by the root aggregator's spring-cloud-dependencies BOM import -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>
		<!-- Version managed by the root aggregator's dependencyManagement -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Boot 4.1 split @AutoConfigureMockMvc and friends out of spring-boot-test-autoconfigure
			 into this dedicated module (org.springframework.boot.webmvc.test.autoconfigure). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Singleton-container pattern (see customer-service's AbstractIntegrationTest), so no
			 testcontainers-junit-jupiter needed. Versions managed transitively by
			 spring-boot-starter-parent's own testcontainers.version property/BOM import. -->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-mongodb</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-rabbitmq</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!--
				*IT.java classes are Testcontainers-backed integration tests (RULES.md Sec9).
				Surefire's default include pattern (*Test.java, *Tests.java, *TestCase.java) skips
				*IT.java on purpose, since that naming convention is Failsafe's, bound to the
				integration-test/verify phases rather than plain `test`.
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
